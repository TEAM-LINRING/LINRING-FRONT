import 'package:flutter/material.dart';

class TagShowScreen extends StatelessWidget {
  const TagShowScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text("Tag Show Screen"),
    );
  }
}
